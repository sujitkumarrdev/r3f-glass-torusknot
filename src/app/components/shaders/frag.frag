// uniform float uTime;
// uniform vec3 uColor;
// varying vec2 vUv;
 
//  void main() {
//     float glow = 0.5 + 0.5 * sin(uTime + vUv.x * 10.0);
//     gl_FragColor = vec4(uColor * glow, 1.0);
//  }